import { assert } from 'chai';
import node  from '../scripts/Node';

describe('Node', () => {
  it('should be a function', () => {
    assert.isFunction(node);
  })

  it('should split words into individual letters', () => {
    
  })
})
